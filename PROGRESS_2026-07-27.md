# 2026-07-27 进展

## 金融数据查询（CC）

新增并接入 `normal_qa` 下的金融专业问答场景。CC 可以在金融问答中读取金融数据工具目录、生成查询协议、执行查询并使用中间结果继续推理。

当前数据源使用 `.env` 中的 `BI_DB_URL`（KingdomAI）。实测该连接正常：MySQL 8.0.28，`kingdomai` 库可访问，包含 224 张表，行情表可读。

## 测试结果

使用 10 个中等到复杂的金融问题进行 CC 全链路测试：

- 10/10 实际返回了业务结果。
- 评测脚本统计为 9/10；唯一未通过的是不存在证券的空结果样本。CC 实际正确返回“证券不存在且不编造”，只是该样本的评测条件要求必须调用 `finance_query`，与实际允许的快捷查询工具不一致。
- 已验证实时行情、历史行情、5/10 日指标、融资余额、增发募集资金、估值比较、多轮实体承接、序数指代和板块关联查询。

复杂查询“最近五日涨幅最高的股票中，主力流入最多的股票”也已跑通：

1. 查询最近 5 日涨幅最高的股票集合；
2. 在候选集合中按 5 日主力净流入排序；
3. 返回锐捷网络（301165.SZ），5 日主力净流入约 12.78 亿元。

## 配置现象

之前的 1044 错误不是 KingdomAI 不可用，而是运行时使用了 `aiia_user` 访问 `kingdomai`。当前阶段应使用 `BI_DB_URL`；未来切换 AIIA 账号时再调整连接映射。

## 证据文件

- `outputs/financial_qa_cc/eval_bi_live_current.json`
- `tests/evals/finance_data_chat_v1.json`
- `scripts/eval_finance_data_chat_e2e.py`

