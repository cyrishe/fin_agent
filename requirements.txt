# -----------------------------
# 基础爬虫与网络请求
requests==2.31.0
beautifulsoup4==4.12.2

# -----------------------------
# Selenium（动态爬虫，需要配合对应的浏览器Driver）
selenium==4.10.0

# 如果想使用Playwright，可替换/补充如下
# playwright==1.38.0

# -----------------------------
# NLP相关
jieba==0.42.1
# 可选：如果使用 HanLP 或 pkuseg, spacy 等，需要自行添加
# hanlp==2.1.0
# pkuseg==0.0.25
# spacy==3.5.3
sentence-transformers==3.0.1

# -----------------------------
# Web 服务
Flask==3.1.2
fastapi==0.115.2
starlette==0.40.0
PyMySQL>=1.1.0,<2.0.0

# 手机号短信持有权验证与可选姓名二要素认证（provider=aliyun 时使用）
alibabacloud_dysmsapi20170525==4.6.0
alibabacloud_dypnsapi20170525==2.0.0
alibabacloud_cloudauth20190307==3.13.1
alibabacloud-tea-openapi==0.4.4

# -----------------------------
# Agent / 大模型调用
openai==1.60.2
openai-codex==0.1.0b3
claude-agent-sdk==0.2.123
# Claude SDK 的 MCP 依赖未限制 sse-starlette 上界；固定与当前 FastAPI/Starlette 兼容的版本。
sse-starlette==2.1.3

# -----------------------------
# 测试 & Mock
pytest==7.4.0
pytest-mock==3.11.1
fastjsonschema==2.21.2
json-repair==0.30.0
# unittest.mock 是内置库，不需要额外安装
# 如果还需要其他测试工具，可在这里补充

# -----------------------------
# 日志 & 配置
PyYAML==6.0  # 用于读取logger_conf.yaml
python-dotenv>=1.0.0,<2.0.0

# -----------------------------
# 其他常用工具库
# (这些库可能根据需要选择性添加)
lxml==4.9.3   # 如果要用 lxml 解析
schedule==1.2.0  # 如果需要 python-schedule 做定时任务
tqdm==4.65.0  # 进度条显示
openpyxl==3.1.5  # 读取 xlsx 文件
python-docx==1.1.2  # 读取 docx 文件
reportlab>=4.2.0,<5.0.0  # 金融研究报告 PDF 导出
pypdf>=5.0.0,<7.0.0  # PDF 导出测试与结构校验
