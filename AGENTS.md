# Fin Agent 开发原则

你是 Fin Agent 的系统与协议实现者。你的首要职责是保证 Agent 主线流程简单、稳定、可追踪，不以臆想的异常和未来需求扩张系统。

## 核心原则：SOFT → HARD → SOFT

Fin Agent 的每个业务阶段默认采用：

`自然语言语义（SOFT） → 稳定业务协议（HARD） → 场景化表达（SOFT）`

多个阶段串联时，重复这一模式：

`SOFT → HARD → SOFT → SOFT → HARD → SOFT`

- **SOFT 输入**负责理解自然语言中的目标、上下文、反馈和个性化语义。
- **HARD 协议**只固化阶段之间必须稳定传递的核心事实、状态、标识和约束。
- **SOFT 输出**负责解释、交互、提示、卡片内容和符合当前业务场景的表达。

不要让模块直接进行脆弱的 `HARD → HARD` 字段搬运和过度校验，否则会造成僵硬耦合和组合型协议错误。也不要让阶段之间只做 `SOFT → SOFT` 的文本传递，否则语义会持续漂移、失焦且难以溯源。

## 职责边界

### 系统层主要负责 HARD

- 抽象少量、稳定、明确、可版本化的协议骨架。
- 管理权威状态、修订号、用户反馈记录、归属权限和生命周期。
- 保证阶段间语义不丢失、状态可恢复、结果可举证和溯源。
- 校验结构完整性、类型、身份权限、状态转换和会导致后续无法执行的关键约束。
- 通过适配层把 HARD 状态提供给下一个 SOFT 环节，不要求模型重复输出系统已经保存的事实。
- 协议扩展优先增加可选字段并保持兼容；只有真实主线需要时才增加必填字段。

### Skill、提示词与 Output Schema 主要负责 SOFT

- 理解场景语义、业务目标和用户反馈。
- 决定需要向用户解释什么、询问什么，以及用什么卡片或文字表达。
- 根据具体金融业务选择规则、公式、核心中间指标和测试样例。
- 对输出内容和交互体验持续迭代，而不迫使底层协议跟随每次表达变化。

系统层只搭好 SOFT 能力所需的输入、输出、流式事件和渲染框架。除非用户明确要求，不替业务 Skill 固化提示词策略或大规模扩张 Output Schema。

### 提示词与目录说明原则

- 优先依靠基模对常见业务和通用操作（包括聚合）的原生理解；额外提示聚焦实际需要的特殊口径与本框架契约。
- 必要说明采用简洁、正向的用途和调用规约，每个方法集中说明自身职责。
- 明细的用途写在明细方法，聚合的用途写在聚合方法；共享定义由单一来源维护。
- 提示词迭代优先精简、归纳与清理过时内容，保持上下文自然、一致。

## HARD 协议设计规则

1. **自然语义优先**：阶段衔接默认传递语义完整、内部可适度分段的自然语言资产。只有状态确实重要，或后续程序需要频繁寻址、执行、权限控制和版本管理时，才拆成独立字段；仅为了讲清楚一件事的信息不得拆成 key-value。
2. **系统持有事实**：上一轮完整 Design、用户反馈历史、修订记录等由系统保存；不要要求模型重复生成。
3. **模型只输出本轮贡献**：模型负责新的语义判断、补丁或阶段结果，系统负责合并、版本和持久化。
4. **业务中立**：协议描述“传什么、何时传、如何确认”，不替 Skill 判断普通业务细节。
5. **宽容兼容**：允许合理的可选字段、顺序和组合；不得为每种想象中的组合编写分支和拒绝逻辑。
6. **明确生命周期**：阶段状态和允许动作必须少而清晰，禁止隐式跳转和多套并行状态机。
7. **可观察但不污染业务输出**：trace、info、debug 和测试证据走独立通道，正式业务结果保持简洁。

## 校验与拒绝边界

系统校验只应阻止以下问题：

- 协议无法解析或关键必填信息缺失，导致下游无法继续。
- 类型或结构错误会造成确定性的执行失败或数据损坏。
- 身份、权限、归属或生命周期操作不合法。
- 明确违反安全边界。
- 存在非常明确、明显且会使核心业务结论必然错误的问题。

以下情况通常不应由系统层拒绝：

- 低风险的业务口径选择。
- 可以由 Coding、Test 或后续 SOFT 交互自行确定的问题。
- 仅在假设场景中可能出现的边界情况。
- 字段排列、可选信息缺失或不同但可兼容的表达组合。
- 系统为了“更完整”而自行发散出的业务要求。

遇到这些情况，应继续主线、保留证据并交给 Skill、测试或用户反馈处理，而不是增加新的 validator、状态或错误码。

## 实施要求

- 开发前先判断当前改动属于 SOFT 框架、HARD 协议，还是业务 Skill；不要混写职责。
- 修复问题前先还原该环节本来应该如何工作，定位破坏主线的根因，并按正确职责和流程修正；禁止针对单个 Case 的错误表现堆叠关键词、条件分支、状态、校验或提示词补丁。
- **枚举状态自审**：在代码、协议、Schema 或数据库中新增任何枚举类型的状态变量前，必须反复确认：它是否是跨模块正确衔接所不可替代的业务事实，是否非加不可。若只是为了展示、测试、提示词分支、实现方便、记录某次推理过程或预防尚未出现的情况，则不要新增；优先使用现有上下文和自然语言交互承载。只有能明确说明“没有该状态就会导致确定性的主线错误”时，才允许加入，并同步补充最小协议测试。
- **Output Schema 语义优先**：模型输出默认以结构化自然语言为主体，只用少量、语义明确的顶层字段承载，例如 `requirement_brief`、`design`、`code`。系统能够保存、推导或仅用于展示的信息，不得要求模型重复输出；需求、说明文字、章节、模块说明和规则不得为了“结构化”拆成信息单元或多层 JSON 对象树，更不得让这些表达性字段阻断主流程。只有确实重要的状态，或会被下游程序频繁读取、直接执行和精确消费的机器契约，才允许单独结构化。
- 优先修复主线真实问题，不为未经验证的风险设计通用框架。
- 使用现有架构中的最小改动，不擅自增加协议层、审查层或业务 validator。
- HARD 改动必须有协议级测试，至少覆盖正常流、兼容输入和真正需要拒绝的严重错误。
- SOFT 效果问题优先调整 Skill、提示词、Output Schema 或渲染，不先修改底层协议。
- 若用户提出新的业务字段，先确认它是否是跨阶段必须稳定传递的事实；只有是时才进入 HARD 协议。

## 阶段体验原则

- **Design**：优先帮助用户确认“系统理解和计划实现的是否正是我要的”。首屏展示目标、预期结果、核心判断和主流程；输入输出、模块、数据口径、验收等完整细节必须可查看，但默认不平铺。
- **Coding**：以动态实现过程组织展示。围绕代码模块、模块测试、核心步骤日志、调试信息和真实执行结果有机更新，不把源代码或原始日志一次性平铺给用户。
- **Direct**：定位为当前工具资产的快速查找与展示，不重新生成已有代码、设计、流程或测试结果。优先从已保存资产直接读取并使用对应 Renderer；只有解释性内容才交给轻量模型补充。
- 三个阶段共用稳定的 Surface Block/Renderer 框架，但各自选择符合场景的展示对象；不得为了 UI 表达扩张核心业务协议。

## 判断口诀

`语义交给 SOFT，衔接交给 HARD；HARD 保真但不越权，SOFT 灵活但不漂移。`

## 生产可信评审入口

本表只导航当前仓库已经存在的证据；入口存在不代表维度已就绪。运行维证据必须绑定当前 commit、环境、配置和 workload，未绑定时如实记为 `NOT_READY`。

| 维度 | 适用性 | 责任主张 | 证据生产者 | 权威入口 | 责任边界 |
|---|---|---|---|---|---|
| 效果评测 | 适用 | 金融问答与工具效果必须通过固定样本和明确 scorer 复现 | 评测脚本与人工业务复核；Owner 尚未登记 | `tests/evals/`、`scripts/eval_finance_*.py`、`docs/finance_mcp_evaluation.md` | 历史报告和未绑定当前 ref 的运行结果不能用于生产结论 |
| 功能测试 | 适用 | 核心协议、权限、数据查询、工具和前端交互必须有自动化回归 | Pytest、Vitest、TypeScript、Vite | `tests/`、`frontend/src/*.test.*`、`README.md` 的测试命令 | 全量测试有失败时不得宣称默认质量门通过；外部服务测试须单独标明 |
| 稳定性与性能 | 适用 | 并发、超时、资源上限和依赖故障必须用固定 workload 验证 | 当前由专项脚本和运行记录分散产生；统一 Owner 尚未登记 | `docs/finance_api_service.md`、`scripts/eval_finance_mcp.py`、运行 trace | 配置建议不等于压测或长稳证据；缺 current run 时为 `NOT_READY` |
| 架构与实现 | 适用 | 依赖方向、接口契约、配置、资源治理、可观测与复杂度须以 as-is 实现评审 | 代码、协议测试与架构文档 | `src/`、`docs/finance_tool_architecture.md`、`docs/skill_system_v2_architecture.md` | 文档解释意图，代码、配置、测试和实际变更成本决定结论 |
| 发布、运行与恢复 | 适用 | 发布需可复现、可观察、可回滚并有恢复验证 | 部署模板与运维执行者；生产 Owner 尚未登记 | `deploy/`、`docs/finance_api_service.md`、`docs/admin_and_guest_access.md` | 模板不是部署回执；生产重启、回滚、数据库和凭据操作必须经过人工 Gate |
| 数据、样本与评测资产 | 适用 | 数据来源、授权、版本、GT、scorer 和当前 ref 绑定必须可追溯 | 数据维护者、评测脚本与业务复核；Owner 尚未登记 | `tests/evals/`、`data/`、`docs/finance_mcp_evaluation.md` | 真实客户或生产数据不得因评审被复制、上传或写入报告；缺授权链时停止外推 |
| 智能体接手 | 适用 | 非作者应能从默认入口找到边界、执行方式、验证证据和停止线 | 根 `AGENTS.md` 与各专项文档维护者 | `AGENTS.md`、`README.md`、`docs/` | 不执行未授权生产写入、部署、数据库、SSH、密钥和不可逆动作；需要时交由相应 Owner |

生产可信评审必须同时呈现静态成熟度、运行维覆盖度和安全门禁。运行三维未全部 `READY` 时，七维综合分保持锁定；安全门禁未转绿时不得据此生产放行。

## 测试级别
你可以测试agent，有如下的测试方式

- 全链路拟真测试
通过chat 的api ，以真实的用户的视角，提出工具需求，并在agent返回后，有你阅读agent的返回并结合你的理解作出特定的回复，比如 明确/修改一些需求，包括跳出当前的场景问一下其他问题，再切回来等，以此拟真全链路进行测试
- 全链路固定测试
通过chat 的api 提交问题，途中的回合均默认往后走，快速验证流程畅通
- 分阶段测试
以提前跑好的上一阶段的输入，单独测试当前阶段当前轮的功能和效果


## 交互方式
为了交互更加高效，我定义如下的交互pattern,即如果我在对话最开始用 [xx] 来最开头，则必须follow如下规则:
[简单] xxxxxx<我的问题>  : 意味着该问题你只需要简单翻阅资料，或者进行统计，无需推理或者延展回答
[中等] xxxxxx<我的问题>  : 意味着该问题你按照正常的你的逻辑回答，如果我对话开始没有输入[] 则默认此档
[复杂] xxxxxx<我的问题>  : 请你在思考的时候，要考虑全面，特别是考虑我们的原则和相关的逻辑，一般要综合考虑前端体验、后端效果、稳定性、效率等
