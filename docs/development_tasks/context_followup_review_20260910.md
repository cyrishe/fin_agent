# 网页多轮指代与显式入口复测（2026-09-10）

## 固定样本与判定

生产网页 Chat/SSE，模型 deepseek-v4-flash-0731，research_mode=auto，无已有会话。按顺序发送：

1. 请按以下顺序列出两家公司最近一篇研报的标题、机构和日期：1.贵州茅台；2.五粮液。请保留编号，暂不展开分析。
2. 第二个怎么样？
3. 和第一个比呢？

三组分别为自动路由、首问显式 equity-report-analysis（后两问普通 Chat）、三问均显式 finance_data_query。每组使用自己的访客 Cookie 和 thread_id。判定以模型实际问题、查询对象和回答为依据，不只看 HTTP/done：第二问应针对五粮液研报做解释或评价，第三问应比较五粮液与贵州茅台；不得把此前“暂不展开分析”当作永久限制。

## 首跑发现

- 自动 thread 4360：第二问被改写为重复查询五粮液研报标题/机构/日期，最终答复称无新需求；第三问才能正确比较。
- Skill thread 4361：第二问重复研报元信息；第三问只比较日期、机构与标题，未回应分析意图。
- 工具 thread 4362：通用数据入口进入独立参数规划，生成不存在的 stock.research_report，且把完整 DSL 作为股票标识解析；出现 needs_input 或工具调用失败。

## 修复（516cbf0）

1. 金融执行输入保留用户原话，已有 resolved_question 作为指代参考，不能代替本轮目标；同步精简指代提示中的任务继承规则。
2. 网页显式通用 finance_data_query 入口与业务方法 Skill 一样，经现有资产解析和授权后交给金融会话。金融 Agent 根据实际目录生成执行请求。底层 DSL 工具执行器未修改；其他具体可执行工具继续使用其已有调用路径。

未新增状态、Schema 或补全/重试层。定向自动化本地 147 项、服务器 53 项通过。原有业务 Skill 目录测试遗漏已存在的 bond-analysis、fund-analysis、capital-flow-analysis，同步更新该断言。

## 修复后公网验证

新会话：自动 4363、Skill 4364、工具 4365。每组第二、三轮均复用首轮 financial_qa.session_id，resumed=true。自动路由第二轮实际加载 equity-report-analysis；显式 Skill 首轮已有专业方法；后续按需读取研报细节。

验证只覆盖这里的指代、问题意图和执行链路，不是全量工具、长对话、跨进程恢复或金融结论准确性的全面验收。原始问题和完整返回保存在工作区 outputs/context_followup_20260910/（首跑）与 after/（修复后），不覆盖失败记录。

三条链路各三轮，9/9 完成；逐条阅读确认第二问分析五粮液研报、第三问比较两家公司研报。

| 入口 | 首问 / 第二问 / 第三问耗时（秒） |
|---|---|
| auto | 17.99 / 21.41 / 26.61 |
| skill | 18.48 / 27.34 / 32.31 |
| tool | 14.41 / 24.82 / 23.85 |
