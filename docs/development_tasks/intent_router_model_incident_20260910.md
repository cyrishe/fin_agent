# 2026-09-10 入口路由 JSON 截断排查

## 根因与范围

截图的普通研报数量问题在自定义工具意图分类阶段失败，尚未执行金融查询。13:45–13:53 的路由日志中，模型正文缺少 JSON 结尾，但 DSH 收到 stop 并将整轮标记 completed。该阶段不依赖注册时间；新会话更容易触发该入口，不是注册权限判断失败。

入口默认模型遗漏了固定版本，使用 DashScope `deepseek-v4-flash`；生产其他金融模型均已配置 `deepseek-v4-flash-0731`。在服务器绕过 DSH 直接读取同一端点原始 SSE，仍重现正文截断并收到两次 stop，因此不是前端或本地 JSON 提取器丢失结尾。尚不能确定上游模型部署/流式网关内部的具体实现原因。

## 定向验证

同一系统提示、同一“贵州茅台近三个月有多少篇研报？”问题和空工具上下文，stream=true、thinking disabled、max_tokens=768，分别执行三次：

| 模型 | 完整且正确的 JSON | total_tokens（逐次） |
|---|---|---|
| deepseek-v4-flash | 0/3 | 359, 355, 359 |
| deepseek-v4-flash-0731 | 3/3 | 360, 355, 358 |

另以生产 DSH SDK/worker 路由（仅将模型设为 0731）验证普通研报查询、行情查询、新建工具、继续修改、退出工具转查数据，5/5 正确，覆盖冷启动与客户端复用。逐次 Token 为 358、347、357、373、369。上述 11 次验证均通过现有 record_request 计入测试桶。它们证明定向恢复，不构成长稳或全量金融效果结论。

修复仅固定入口默认模型，更新当前配置示例、调用脚本和运维说明；保留现有 JSON 协议、允许完整 fenced JSON、拒绝截断响应，不添加猜测补全、关键词路由或重试层。历史模型评测记录保持其原始模型标识。
