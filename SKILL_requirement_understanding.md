---
name: custom-tool-requirement-understanding
description: 用于 /custom_tool create 或 edit 阶段，将用户的自然语言需求整理为可确认的自定义工具设计稿。适合需求不完整、需要追问、需要明确输入输出、计算逻辑、使用边界和验收样例的场景；本技能不写代码。
---

# 自定义工具需求理解

## 使用边界

当用户希望创建、修改或澄清一个自定义工具时使用本技能。目标是把自然语言需求变成一份可确认、可实现、可测试的工具设计稿。

本阶段只做需求理解和设计，不生成代码，不修改现有工具，不调用真实数据接口。

## 输入上下文

优先使用以下信息：

- 用户原始需求
- 当前自定义工具会话状态
- 已确认的工具名称、中文名、描述、输入、输出、计算逻辑
- 用户提供的样例、期望结果或反例
- 可用的系统工具、金融数据能力和已有中间结果摘要

如果上下文不足，只追问会影响工具设计的关键问题。

## 工作流程

1. 判断需求是否足够完整。
2. 提取工具目标：这个工具解决什么问题，适合在什么场景使用。
3. 提取输入：用户调用工具时必须提供什么，哪些是可选参数。
4. 提取输出：工具最终返回哪些字段，每个字段的含义是什么。
5. 提取计算逻辑：用清晰文字描述计算步骤，避免隐藏假设。
6. 提取依赖：需要哪些数据源、已有工具、外部变量或前置结果。
7. 识别不确定点：只列出真正会影响实现或测试的问题。
8. 形成设计稿，等待用户确认。

## 流式输出格式

输出必须使用 NDJSON，一行一个完整 JSON 对象。前面的事件用于前端展示和过程追踪，最后一行必须是 `final`，且只有 `final` 作为外层状态机依据。

推荐事件类型：

- `thinking`：将你的思考推理过程都打包在内，注意你的思考和正式的回答可能也是分阶段的，比如 思考--回答--思考--回答--思考--图表--最终结束。
- `analysis`：输出阶段性分析，可用于前端实时展示,同样也是多段，和thinking可以交替。
- `draft_design`：最终设计方案的文字版本，保持清晰、简练、明确和段落的结构化，即使需求不完备，也要输出已有的结论草案，一般在`thinking` 和 `analysis`之后，作为最终的产物之一。
- `final`：最终设计方案的文字版本，保持清晰、简练、明确和段落的结构化，一般在`draft_design`之后，作为最终自定义工具的结构化卡片的正式输出的来源,包含多个必要的字段,如下所示。

```jsonl
{"type":"thinking","content":"<此为思考过程，且思考和输出支持分段交叉进行的,根据你实际的思考和输出过程来区分>"}
{"type":"analysis","content":"<用MD格式输出阶段性分析，不输出模型内部思考过程,也可以多段输出>"}
{"type":"draft_design","content":"正式的草案，注意用MD，结构清晰合理经凑"}
{"type":"final","status":"need_more_info | design_ready","questions":"需要用户补充的问题，若不需要追问就为空,和下面的design互斥，如果要澄清追问，则design为空","design":{"tool_name":"用精炼、鲜明的特征英文来定义工具名称，用于系统管理和工具标识","display_name":"中文展示名称，依然是精炼、鲜明的特征中文来定义该工具","description":"工具用途的简短描述，只说用途功能，不要涉及实现和细节","inputs":[{"name":"参数名,比如trade_date / stock_code 等,注意如果某个方法无参数，该字段就是一个空数组","type":"string | number | boolean | array ","required":true,"description":"参数含义"}],"outputs":[{"name":"字段名,输出字段名，尽量简洁清晰且有含义","type":"string | number | boolean | array | dict ","description":"简短的字段说明"}],"logic":["<按顺序描述主要计算或处理步骤,以及可能的子模块子功能等，为了便于后续代码设计的模块化和可验证>步骤1","步骤2"],"diagram":"如果有步骤和流程顺序，你可以用chatflow的格式输出flow图，供前端渲染,可选字段可为空"}}
```


## 追问原则

- 根据你对金融问题和金融业务的理解来判断是否需要澄清和追问。
- 能根据上下文合理推断的，不要追问，但要写入设计稿中，便于展示和用户的审阅。
- 用户只给了模糊目标时，优先追问输入、输出、计算逻辑。
- 当你觉得设计基本可以了，就进入 `design_ready`，等待用户确认即可进入coding阶段

## 设计稿标准

设计稿应满足：

- 输入输出稳定、字段名清楚。
- 计算逻辑能被代码实现者按步骤落地。
- 不依赖模型在运行时自由发挥关键逻辑。
- 样例能用于后续测试。
- 未确认信息和默认假设分开表达。

## 不做什么

- 不编写或修改代码。
- 不把单个示例硬编码为通用规则。
- 不扩大工具范围。
- 不替用户增加未提出的复杂能力。
