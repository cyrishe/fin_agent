# CC 重建基准封存验证

验证日期：2026-09-08。结论：**旧 CC 金融问答子链路可以从独立封存目录完整运行，未混入当前 DSH 代码。** 这不是9月2日准确率或性能的复现。

## 完整性与隔离

- 714个源码/静态文件全部来自 `2242990`，未覆盖当前工作区内容；可重复构建得到同一归档 SHA256：`c0ecf83c4f5e400ef1d94ec8745562ab0e82b48ff1c2b90a942c4d6b0d963ac6`。
- 9月2日20题完整结果逐值保留；独立审计和原文件哈希见 `history/`。
- 新建独立 Python 环境，从64个锁定依赖安装；`pip check` 通过。CLI字节与现有2.1.215一致。
- 从封存目录内的固定入口执行离线检查和真实烟测；100个已加载 `src` 模块均位于本次新解包的 workspace。提示词、目录、Skill和应用角色也从旧源码读取。
- 离线检查禁止网络连接及启动CLI，依赖、目录与角色初始化全部通过；真实调用使用新的本地数据、日志、会话目录，不接入系统身份库。
- 从封存目录独立运行归档、启动器和历史证据测试：29项通过、2项跳过。跳过项仅依赖未入Git的原始日志；封存内容自身的完整性检查通过。有1条第三方弃用警告。

## 四道原题的真实烟测

由于本机 DeepSeek 官网 Key 为空，本次**显式**使用百炼 `deepseek-v4-flash-0731`，low、12轮、auto、完整回答。与历史模型来源不同；按题顺序执行，也不同于历史并发3。

| 原题 | 实际数据路径与条数 | 完整返回 | 耗时 | Token |
|---|---|---|---:|---:|
| RTE055：阳光电源2026–2028年营收增速预测 | 研报指标76条 | 是 | 34.74秒 | 11,625 |
| RTE003：鹏鼎控股与沪电股份2026年净利增速差异 | 研报指标17＋20条 | 是 | 21.49秒 | 9,470 |
| RTE073：长江电力估值溢价依据 | 估值1、三张表8、研报10、分红5条 | 是 | 57.36秒 | 25,269 |
| RTE093：中国中免股价波动与竞争的关系 | 行情130、三张表10、业务分部20条 | 是 | 75.75秒 | 34,472 |

合计189.35秒，80,836 Token。四题旧运行时记录均 `ok=true`、无错误，并完整返回数据和自然语言答案。Token是旧运行时报告的请求级总量，不冒称每个内部回合的独立精确计费。

**运行通过不等于答案全部正确。** RTE055本次选明细，历史选聚合；RTE073展开了较多辅助数据；RTE093本次未选研报，与历史“行情＋研报”路径不同。这里仅确认固定旧代码可完成完整流程，保留这些模型选择与回答差异，不为烟测改旧提示词或修旧答案。

## 启动器修复记录

首次烟测中，前两题旧CC已经完成取数和答案生成，但启动器使用字符串会话ID，旧展示层执行 `int(thread_id)` 时失败。已把启动器改为隔离 workspace 内的数字ID，并增加旧展示层协议测试；**没有修改封存源码**。随后上表四题从头重跑全部完整返回。

首次诊断保留在 `outputs/cc_reference_rebuilt_smoke4_20260908/`；最终烟测为 `outputs/cc_reference_rebuilt_smoke4_verified_20260908/`。封存 `verification/` 保留最终完整结果、运行时记录、环境与来源哈希，未复制运行环境中的凭据或Claude登录文件。

## 后续使用

日常比较使用本目录 `tools/run_cc_reference.py`，不要使用会继续变化的开发目录脚本。默认仍是历史 DeepSeek 官网配置，需要外部注入官网 Key 才能线上验证该路线。旧数据库/模型服务的外部变化、顶层Chat路由、HTTP并发和前端UI不属于本次已复现范围。详见本目录 README。
