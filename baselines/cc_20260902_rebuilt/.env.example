# External credentials only. Copy this OUTSIDE the baseline and fill it locally.
# The launcher fixes the CC runtime to low / 12 turns / auto / full answer.
# Default: DeepSeek official Anthropic-compatible API, deepseek-v4-flash.
DEEPSEEK_API_KEY=

# Optional explicit verification variant: --provider dashscope --model deepseek-v4-flash-0731
DASHSCOPE_API_KEY=

# Finance data only: no system/identity DB, no copied personal Claude/Codex auth.
KINGDOMAI_DB_HOST=47.94.1.2
KINGDOMAI_DB_PORT=3312
KINGDOMAI_DB_USER=
KINGDOMAI_DB_PASSWORD=
# Alternatively KINGDOMAI_DB_URL=mysql+pymysql://USER:PASSWORD@HOST:PORT/kingdomai
